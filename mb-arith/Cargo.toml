[package]
name = "mb-arith"
version = "0.1.0"
edition = "2021"
authors = ["Bill Wood <wpwoodjr@gmail.com>"]

[dependencies]
num = "*"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[features]
# wasm32 SIMD128 kernels for the u32 limb engine; requires a SIMD-capable
# wasm engine (all major browsers since 2021, Safari since 16.4)
simd128 = []
